const {
  ContactSchema,
  TalktosalesSchema,
  RequestsampleSchema,
} = require('./form.schema');

const { MailSchemas } = require('./mail.schema');

module.exports = {
  MailSchemas,
  ContactSchema,
  TalktosalesSchema,
  RequestsampleSchema,
};
