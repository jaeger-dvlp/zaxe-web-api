const { main, knowledgeBase, careers, xdesktop } = require('./form.schema');

const { MailSchemas } = require('./mail.schema');

const WarrantySchema = require('./warranty.schema');

module.exports = {
  MailSchemas,
  main,
  knowledgeBase,
  careers,
  xdesktop,
  WarrantySchema,
};
